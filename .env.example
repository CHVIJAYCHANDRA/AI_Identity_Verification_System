OPENAI_API_KEY=

# Vision model. Must support image input.
OPENAI_VISION_MODEL=gpt-4o

# Reject uploads above this size before spending an API call.
MAX_UPLOAD_MB=8

# Below this the app returns "inconclusive" rather than a verdict.
MIN_EXTRACTION_CONFIDENCE=0.6
